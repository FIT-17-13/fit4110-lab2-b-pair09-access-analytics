# Known Issues — Lab 02

Ghi các lỗi còn tồn tại nếu chưa xử lý xong.

| Lỗi | Ảnh hưởng | Cách xử lý dự kiến | Người phụ trách |
|---|---|---|---|
| Tài khoản GitHub của Leader nhóm Consumer bị lỗi phân quyền **403 Permission Denied** khi push code lên Repo chung của tổ chức lớp (`...-pair09-access-analytics`). | Nhóm 5 (Analytics) không thể trực tiếp đóng góp file Markdown và mã nguồn Mock Server vào Repo chính thức của lớp, có nguy cơ ảnh hưởng đến tiến độ chấm điểm tự động trên GitHub Classroom. | **Giải pháp tạm thời:** Tạo một Repository cá nhân Public tạm thời để lưu trữ toàn bộ code, file thiết kế và chạy sinh minh chứng.<br>**Giải pháp dứt điểm:** Nhờ đại diện Nhóm 9 (Access Gate) add tài khoản GitHub của Kiều Quang Trường làm *Collaborator*, hoặc nhờ bạn nhóm 9 hỗ trợ chạy lệnh Git Merge kéo toàn bộ lịch sử commit từ repo cá nhân về repo chung của lớp trước giờ nộp bài. | Kiều Quang Trường & Đại diện Nhóm 9 |
| Chênh lệch môi trường chạy Mock Server (Nhóm dùng **FastAPI** trên Python thay vì công cụ giả lập tự động **Prism** bằng Node.js như gợi ý ban đầu của học phần). | Có thể phát sinh cảnh báo nhỏ về tính tương thích hoặc sai khác cách serialize kiểu dữ liệu thời gian (DateTime) giữa môi trường kiểm thử cục bộ và hệ thống test tự động của trường. | **Cách xử lý:** Nhóm 5 đã chủ động ép kiểu dữ liệu đầu ra bằng Pydantic map khớp 100% theo các schema `Problem` và `AccessEvent` trong file `openapi.yaml`. Sang buổi Lab sau khi kết nối Message Queue thật, nhóm sẽ gỡ bỏ hoàn toàn Mock Server này để chuyển sang viết code xử lý logic nghiệp vụ chạy ngầm. | Nhóm 5 (Analytics) |